<head>
  <title>Rides Page</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.7/lumen/bootstrap.min.css">
  <link href="../public/stylesheets/style.css" rel="stylesheet">
</head>

<body id="postBackground">
  <nav class="navbar navbar-default navbar-fixed-top" id="nav1">
    <div class="container">
      <!-- <p class="float-right"><a id="login" href="/logout">Log out</a></p> -->
      <div class="navbar-header">
        <h3 id="f0">SCARLET <img id="ruLogo" src="http://content.sportslogos.net/logos/33/817/full/6853_rutgers_scarlet_knights-alternate-2001.png" alt="leaf" style="width:35px;">IDES</h3>
        <a class="navbar-brand" href="/members">Menu</a>
        <a class="navbar-brand" href="/accountInfo">Account</a>
        <a class="navbar-brand" href="/logout">Log out</a>
      </div>

    </div>
  </nav>
  <br><br><br><br><br><br><br><br>

  <div class="container absolute">
    <div class="row">
      <div class="col-md-6 shift">
        <h2 class="fontHere" id="ridesAvailable">Rides Available</h2>
        {{#each posts}}
          {{#if this.carSeats}}

              <div class="ride-form">
                <p class="fontHere"><b>Driver: </b> {{this.first}} {{this.last}}</p>
                <p class="fontHere"><b>From: </b> {{this.from}} <b>To: </b> {{this.to}}</p>
                <p class="fontHere"><b>Posted on: </b>{{this.createdAt}}</p>
                <p class="fontHere"><b></b> {{this.notes}}</p>
                <p class="fontHere"><b>Meet at: </b> {{this.time}}</p>
                <!-- <p class="fontHere"><b>ID: </b> {{this.id}}</p> -->
                <p class="fontHere"><b>Seats Available: </b> {{this.carSeats}}</p>
                <br>
                <div class="form-group">
                  <label for="startCampus"><b>Pick number of seats</b></label>
                  <select class="form-control" id="seatInput">
      					    <option value="1">1 (One)</option>
      					    <option value="2">2 (Two)</option>
      					    <option value="3">3 (Three)</option>
                    <option value="4">4 (Four)</option>
        					</select>
                </div>

                <form data-id={{this.id}} data-seats={{this.carSeats}} class="pickRide">
                  <input input type="hidden" type="text" id="chosen_{{this.id}}" name="post_id" value={{this.id}}>
                  <button type="submit" data-trigger="save" class="btn btn-default fontHere">Choose</button>
                </form>
              </div>

          {{/if}}
        {{/each}}

      </div>
      <!-- <div class="col-md-6">




              <div class="selected-form">
                <p class="fontHere"><b>Driver: </b> {{this.first}} {{this.last}}</p>

                <form class="cancel-form">
                  <input input type="hidden" type="text" id="cancelbtn" name="post_id" value={{this.id}}>
                  <button type="submit" class="btn btn-default fontHere">Cancel</button>
                </form>

                <form class="confirm-form">
                  <input input type="hidden" type="text" id="confirmbtn" name="post_id" value={{this.id}}>
                  <input input type="hidden" type="text" id="conFirst" name="post_id" value={{this.first}}>
                  <input input type="hidden" type="text" id="conLast" name="post_id" value={{this.last}}>
                  <input input type="hidden" type="text" id="conFrom" name="post_id" value={{this.from}}>
                  <input input type="hidden" type="text" id="conTo" name="post_id" value={{this.to}}>
                  <input input type="hidden" type="text" id="conNotes" name="post_id" value={{this.notes}}>
                  <input input type="hidden" type="text" id="conTime" name="post_id" value={{this.time}}>
                  <button type="submit" class="btn btn-default fontHere">Confirm</button>
                </form>

                <p class="fontHere"><b>From: </b> {{this.from}} <b>To: </b> {{this.to}}</p>
                <p class="fontHere"><b>Posted: </b>{{this.createdAt}}</p>
                <p class="fontHere"><b>Notes: </b> {{this.notes}}</p>
                <p class="fontHere"><b>Time: </b> {{this.time}}</p>
                <p class="fontHere"><b>Email: </b> {{this.email}}</p>
              </div>


      </div> -->
    </div>
  </div>


  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <script type="text/javascript" src="/js/search.js"></script>

</body>
